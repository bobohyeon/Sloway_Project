spring.application.name=app

server.port=8080

spring.profiles.include=dev,test,private,cloud,secret

spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/sloway
spring.datasource.username=postgres
spring.datasource.password=postgreskh
spring.flyway.enabled=true

#JPA
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

spring.jpa.hibernate.ddl-auto=update

#spring.jpa.hibernate.ddl-auto=create

# spring.jpa.hibernate.ddl-auto=create-drop

# spring.jpa.hibernate.ddl-auto=validate

app.base-url=https://api.sloway.store/
app.frontend-url=https://www.sloway.store/

# 1. ?? ??? ?? ?? ?? (???: 10MB)
spring.servlet.multipart.max-request-size=50MB

# 2. ?? ?? ??? ?? ?? ?? (???: 1MB)
spring.servlet.multipart.max-file-size=30MB
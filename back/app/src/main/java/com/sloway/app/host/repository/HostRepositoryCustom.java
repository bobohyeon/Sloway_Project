package com.sloway.app.host.repository;

/**
        * 호스트 복잡 쿼리 확장 인터페이스 (팀 공통 패턴).
        *
        * <p>현재는 단순 조회만 필요해 비어 있음. 구조 일관성 위해 유지.
        * 나중에 "승인대기 호스트 목록 + 신청일 정렬 + 동적 필터" 같은
 * 복잡 쿼리 생기면 여기 선언하고 HostRepositoryImpl에서 구현.
 */
public interface HostRepositoryCustom {
}

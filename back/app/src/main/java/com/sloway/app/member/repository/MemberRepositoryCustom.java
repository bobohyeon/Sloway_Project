package com.sloway.app.member.repository;

/**
 * 회원 복잡 쿼리 확장 인터페이스 (팀 공통 패턴).
 *
 * <p>JpaRepository 메서드 이름 규칙으로 표현 불가능한 동적/복잡 쿼리를
 * 여기 선언하고 MemberRepositoryImpl에서 구현.
 * 현재는 단순 조회만 필요해 비어 있음 (구조 일관성 위해 유지).
 */
public interface MemberRepositoryCustom {
}
